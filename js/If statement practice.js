var user = "jonlee012";
var isLoggedIn = true'
var isLoggedIn = true;
if (user == "jonlee012" && isLoggedIn == true); {
    console.log ("you have access") }
if (user == "jonlee012" && isLoggedIn == true); {
    alert ("are you sure?") }
if (user == "jonlee012" && isLoggedIn !== true); {
    console.log ("incorrect credentials") }
